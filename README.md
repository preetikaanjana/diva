# Diva

A React (Create React App) client hosted in client/.

## Run locally

1. cd client
2. npm install
3. npm start

## Build

npm run build (in client/)

## Notes
- Server and legacy static files removed.
- Env files are ignored via .gitignore. Do NOT commit .env.
